{
  "mcpServers": {
    "saccos-database": {
      "command": "npx",
      "args": [
        "-y",
        "@executeautomation/database-server",
        "--postgresql",
        "--host", "127.0.0.1",
        "--database", "saccos_core",
        "--user", "postgres",
        "--password", "postgres",
        "--port", "5432",
        "--ssl", "false"
      ]
    }
  }
}
